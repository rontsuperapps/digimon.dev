{
  "name": "digimon-frontend",
  "version": "3.0.0",
  "private": true,
  "description": "DIGIMON — Digital Monitoring (PWA). Frontend statis, styling Tailwind CSS v4.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/app.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./assets/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "@tailwindcss/forms": "^0.5.9",
    "tailwindcss": "^4.1.0"
  }
}
